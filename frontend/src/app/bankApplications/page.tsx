import CardApp from "@/components/cardApp/CardApp";

  
  export default async function Page() {

    const appHandler = (num) => {
      if(){

      }
    }

    return (
      <main>
        <button onClick={appHandler(1)}>
          Apply For A Credit Card
        </button>
        <button onClick={appHandler(2)}>
          Apply For A Loan
        </button>
      </main>
    );
  }